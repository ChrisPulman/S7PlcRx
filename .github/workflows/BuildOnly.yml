# ------------------------------------------------------------------------------
# <auto-generated>
#
#     This code was generated.
#
#     - To turn off auto-generation set:
#
#         [GitHubActions (AutoGenerate = false)]
#
#     - To trigger manual generation invoke:
#
#         nuke --generate-configuration GitHubActions_BuildOnly --host GitHubActions
#
# </auto-generated>
# ------------------------------------------------------------------------------

name: BuildOnly

on:
  push:
    branches-ignore:
      - main

env:
  CI: true

jobs:
  windows-latest:
    name: windows-latest
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Setup .NET (With cache)
        uses: actions/setup-dotnet@v5.0.1
        with:
         dotnet-version: |
            8.0.x
            9.0.x
            10.0.x
         include-prerelease: true
         cache: true
         cache-dependency-path: |
           **/Directory.Packages.props
           **/*.sln
           **/*.csproj
           **/global.json
           **/nuget.config

      - name: 'Cache: .nuke/temp, ~/.nuget/packages'
        uses: actions/cache@v5
        with:
          path: |
            .nuke/temp
            ~/.nuget/packages
          key: ${{ runner.os }}-${{ hashFiles('**/global.json', '**/*.csproj', '**/Directory.Packages.props') }}
      - name: 'Run: Compile'
        run: ./build.cmd Compile

      - name: Test with Microsoft Code Coverage
        run: |
            dotnet tool install --global dotnet-coverage
            dotnet-coverage collect "dotnet test src/S7PlcRx.Tests/S7PlcRx.Tests.csproj -f net8.0 --no-build --configuration Release" -f cobertura -o coverage.cobertura.xml

      - name: Upload coverage artifact
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          path: coverage.cobertura.xml

      - name: Publish coverage summary
        uses: irongut/CodeCoverageSummary@v1.3.0
        with:
          filename: coverage.cobertura.xml
          badge: true
          format: markdown
          output: both
